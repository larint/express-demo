extends ../layout
include ../partials/paginate.pug

block content
    table.table
        each item in data.data
            tr
                td
                    span=item.title
                td
                    -var date = new Date(item.created_at)
                    span=date.toLocaleDateString("en-US")
                td
                    a(href="/news/"+item.id+"/edit")
                        button.btn.btn-sm.btn-secondary sửa
    +pagination(data)